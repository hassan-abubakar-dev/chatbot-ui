# Chatbot UI
A simple React-based chatbot UI with basic conversation logic using the `supersimpledev` Chatbot library.

## Features
- Auto-scroll to latest message
- Simple AI-like response system
- Timestamped chat messages
- Clean responsive UI

## Run locally
```bash
npm install
npm start
